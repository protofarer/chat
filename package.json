{
  "name": "wsmin",
  "version": "1.0.0",
  "description": "learning websockets ws lib",
  "main": "index.js",
  "scripts": {
    "dev": "DEBUG=express-session concurrently 'vite --host' 'nodemon server.js'",
    "start": "DEBUG=vite* vite serve --host",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "author": "Kenny",
  "license": "MIT",
  "dependencies": {
    "cors": "2.x",
    "dotenv": "16.x",
    "express": "4.x",
    "express-session": "1.x",
    "isomorphic-ws": "4.x",
    "uuid": "8.x",
    "ws": "8.x"
  },
  "devDependencies": {
    "concurrently": "7.x",
    "eslint": "8.x",
    "nodemon": "2.x",
    "vite": "3.x"
  }
}
